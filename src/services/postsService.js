// Regras de negócio dos Posts
// Responsável: RENAN(CRUD)

const postsService = {
  create(post) {
    // criar post
  },

  findAll() {
    // listar posts
  },

  findById(id) {
    // buscar post por ID
  },
};
// etc...

module.exports = postsService;
