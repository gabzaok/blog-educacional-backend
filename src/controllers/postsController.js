// Controlador das requisições HTTP
// Responsável: RENAN (CRUD)

const postsController = {
  getPosts(req, res) {
    // GET /posts
  },

  getPostById(req, res) {
    // GET /posts/:id
  },
};
//  etc...

module.exports = postsController;
